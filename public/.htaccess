RewriteEngine On 
RewriteBase /
#accept loading of actual files and directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

#send everythin else to the index page
RewriteRule ^ index.php [L,QSA]
